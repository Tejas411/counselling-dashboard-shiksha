# TODO

Tasks assigned in this project, newest first.

Call log over-writen - fix it
Add free/paid applications
Add data_refresh rules - incremental or daily full refresh
Add delivery data in applications

---

## Done

- [x] Add Top 15 Base Courses widget (horizontal stacked bar, highest at top)
- [x] Remove All Records table widget; add Application Submission Date daily trend; fix Top 15 Colleges sort (highest at top)
- [x] Fix dashboard blank/broken — `tickfont` duplicate kwarg in callback causing 500 error

- [x] Build Plotly Dash dashboard from Excel report (`app.py`) — interactive charts with filters
- [x] Apply clean modern minimalistic theme — Inter font, near-black palette, zero gridlines, `assets/style.css`
- [x] Configure permission allowlist (`.claude/settings.json`) to reduce repeated prompts
- [x] Create `CLAUDE.md` with architecture overview and run commands
